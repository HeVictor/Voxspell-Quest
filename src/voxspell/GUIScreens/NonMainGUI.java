package voxspell.GUIScreens;

import voxspell.Command;

/**
 * Originally by jacky, changed the name by Victor so that only non main GUI classes implements this.
 *
 * @author jacky lo
 */
public interface NonMainGUI {

    public void setModel(Command cmd);
}
